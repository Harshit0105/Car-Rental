import { MyCategoryPipe } from './my-category.pipe';

describe('MyCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new MyCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
